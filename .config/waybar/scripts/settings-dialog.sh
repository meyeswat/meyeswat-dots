#! /usr/bin/bash
$terminalEmulator
echo These options configure the look and feel of your hyprland installation.
sleep 1
echo You can change the theme and wallpaper or access the hyprland config directly.
sleep 1
echo Themes are changed using pywal16.
sleep 1
echo Wallpapers are changed using hyprpaper.
sleep 1
echo The hyprland config is accessed via the file manager Thunar.
echo It will automatically open a window to the .config folder for hyprland.
